# Исправление ошибки "RuntimeError: lost sys.stdin"

## 🐛 Проблема:

При запуске .exe файла возникала ошибка:
```
RuntimeError: lost sys.stdin
```

**Причина:** Функция `input()` не работает в .exe собранном с `console=False` (без консольного окна).

---

## ✅ Решение:

### Что исправлено:

1. **launcher.py** переписан для использования GUI диалогов вместо `input()`
   - При первом запуске показывается **GUI диалог** с вопросом об установке браузеров
   - Используются PyQt5 QMessageBox для всех диалогов
   - Ошибки также показываются через GUI

2. **build_exe_debug.spec** - создана debug версия
   - С консольным окном для отладки
   - Удобно для разработчиков

---

## 📋 Что изменилось для пользователей:

### Раньше (с ошибкой):
```
❌ Программа вылетала с ошибкой "RuntimeError: lost sys.stdin"
```

### Теперь (исправлено):
```
✅ При первом запуске появляется красивый GUI диалог:

   ┌─────────────────────────────────────────┐
   │  First Run Setup                      [?]│
   ├─────────────────────────────────────────┤
   │  Playwright browsers are not installed.  │
   │                                           │
   │  They are required for some features     │
   │  (Renamer, Greeting, Obrezka tabs).      │
   │  Download size: ~300MB                   │
   │                                           │
   │  Install now?                            │
   │                                           │
   │           [ Yes ]    [ No ]              │
   └─────────────────────────────────────────┘
```

---

## 🔨 Технические детали:

### Изменения в launcher.py:

#### 1. Функция `install_playwright_browsers()`:
- ✅ Добавлена поддержка PyQt5 диалогов
- ✅ Показывает GUI запрос на установку
- ✅ Показывает прогресс установки
- ✅ Fallback на автоматическую установку если PyQt5 недоступен

#### 2. Функция `setup_first_run()`:
- ❌ Убран `input()` полностью
- ✅ Автоматическая проверка и установка

#### 3. Функция `main()`:
- ✅ Ошибки показываются через QMessageBox
- ✅ Если GUI недоступен - сохраняются в лог файл
- ✅ Попытка автоматически открыть лог файл

---

## 📝 Дополнительно создано:

### build_exe_debug.spec
Debug версия для разработчиков с консольным окном:
```bash
pyinstaller build_exe_debug.spec
# Результат: ITNELEP_Tools_Debug.exe (с консолью)
```

**Преимущества debug версии:**
- Видны все print() сообщения
- Можно использовать input() если нужно
- Удобно для отладки проблем

---

## 🚀 Как пересобрать:

```bash
# Обычная версия (без консоли, с GUI диалогами)
build_exe.bat

# Debug версия (с консолью)
pyinstaller build_exe_debug.spec
```

---

## ✨ Преимущества исправления:

1. **Нет ошибок** - программа запускается без проблем
2. **GUI диалоги** - красивые и понятные для пользователей
3. **Автоматизация** - минимум действий от пользователя
4. **Обработка ошибок** - понятные сообщения об ошибках
5. **Лог файлы** - если GUI недоступен, ошибки сохраняются в файл

---

## 📋 Что нужно пересобрать:

После получения обновленного архива:

```bash
# 1. Установите зависимости (если еще не установлены)
pip install -r requirements.txt
pip install pyinstaller

# 2. Пересоберите .exe
build_exe.bat

# 3. Готово! Новый ITNELEP_Tools.exe без ошибок
```

---

## 🎯 Итог:

Проблема **полностью решена**. Теперь .exe файл:
- ✅ Запускается без ошибок
- ✅ Показывает красивые GUI диалоги
- ✅ Автоматически устанавливает браузеры при согласии пользователя
- ✅ Понятно сообщает об ошибках
- ✅ Готов к распространению
