# 📦 Создание .exe файла

## 🎯 Что получится:

Один файл **ITNELEP_Tools.exe** (~150-200 MB), который:
- ✅ Содержит Python и все библиотеки
- ✅ Работает на любом Windows PC (без установки Python)
- ✅ При первом запуске установит Playwright браузеры (если нужно)
- ✅ Можно передавать на другие устройства

---

## 🛠️ Как создать .exe (на компьютере с Python):

### Способ 1: Автоматическая сборка (Рекомендуется)

1. **Откройте папку с проектом**

2. **Запустите `build_exe.bat`:**
   ```bash
   build_exe.bat
   ```

3. **Подождите ~5-10 минут** (PyInstaller соберёт всё)

4. **Готово!** Файл `ITNELEP_Tools.exe` создан

### Способ 2: Ручная сборка

```bash
# 1. Установить PyInstaller
pip install pyinstaller

# 2. Собрать .exe
pyinstaller build_exe.spec

# 3. Взять готовый файл
copy dist\ITNELEP_Tools.exe .
```

---

## 📋 Требования для сборки:

На компьютере где собираете .exe должно быть установлено:
- ✅ Python 3.8+
- ✅ Все зависимости из requirements.txt
- ✅ PyInstaller: `pip install pyinstaller`

**Важно:** Собирать нужно на Windows для Windows!

---

## 🚀 Использование готового .exe:

### Первый запуск:

1. **Скопируйте файл** на любой Windows ПК

2. **Запустите ITNELEP_Tools.exe**

3. **При первом запуске:**
   - Появится GUI диалог: "Playwright browsers are not installed"
   - Нажмите `Yes` для установки (рекомендуется)
   - Или `No` если хотите пропустить (некоторые функции не будут работать)
   - Подождите 2-3 минуты пока установятся браузеры (~300MB)
   - Появится сообщение "Success"

4. **Откройте настройки** (Ctrl+H)
   - Заполните логин и пароль
   - Укажите ID Google таблицы
   - Сохраните

5. **Готово!** Все вкладки работают

### Последующие запуски:

Просто запустите `ITNELEP_Tools.exe` - всё уже настроено!

---

## 📁 Что нужно передать вместе с .exe:

### Минимум:
- ✅ `ITNELEP_Tools.exe` (основной файл)

### Для полной работы:
- ✅ `ITNELEP_Tools.exe`
- ✅ `service_account.json` (для Google Sheets)

**Важно:** `service_account.json` должен быть в той же папке что и .exe!

---

## ⚙️ Настройки после переноса:

После копирования .exe на новый ПК:

1. Запустите программу
2. Нажмите `Ctrl+H`
3. Заполните настройки:
   - Логин и пароль ITNELEP
   - ID Google таблиц
   - API ключи (если нужны)
4. Сохраните

Настройки сохранятся в:
- Windows: `%APPDATA%\ITNELEP_Tools\config.json`

---

## 🔧 Troubleshooting

### Проблема: "ITNELEP_Tools.exe не запускается"

**Решение:**
1. Запустите от имени администратора
2. Добавьте в исключения антивируса
3. Проверьте что `service_account.json` в той же папке

### Проблема: "Failed to install Playwright browsers"

**Решение:**
1. Проверьте подключение к интернету
2. Запустите от имени администратора
3. Вручную: откройте CMD в папке с .exe и выполните:
   ```bash
   ITNELEP_Tools.exe --install-browsers
   ```

### Проблема: "Antivirus blocks the file"

**Решение:**
Это нормально для .exe собранных PyInstaller. Добавьте в исключения:
- Windows Defender: Параметры → Защита от вирусов → Исключения
- Добавьте файл `ITNELEP_Tools.exe`

---

## 📊 Размеры файлов:

| Компонент | Размер |
|-----------|--------|
| ITNELEP_Tools.exe | ~150-200 MB |
| Playwright browsers (при первом запуске) | ~300 MB |
| **Итого на диске** | ~500 MB |

---

## ✨ Преимущества .exe версии:

- ✅ **Портативность:** Работает на любом Windows ПК
- ✅ **Нет зависимостей:** Python не нужен
- ✅ **Простота:** Один файл вместо папки с кодом
- ✅ **Автоустановка:** Браузеры ставятся при первом запуске

---

## 📝 Примечания:

1. **Первый запуск займёт больше времени** из-за установки браузеров
2. **Антивирусы могут ругаться** - это нормально для PyInstaller
3. **Файл большой** (~150-200MB) потому что содержит весь Python
4. **service_account.json обязателен** для работы с Google Sheets
5. **Настройки персональные** - на каждом ПК свой config.json

---

## 🎯 Альтернативные варианты:

### Если .exe слишком большой:

Вместо одного .exe можно создать **Portable версию**:
- Папка с Python + все библиотеки
- Размер: ~100MB (без браузеров)
- Запуск через `start.bat`

### Если нужна облегченная версия:

Можно убрать некоторые библиотеки из сборки:
- Без OpenAI - вкладка "Генератор" работает без AI улучшений
- Без pymorphy2 - упрощенная морфология

---

## 🚀 Быстрый старт для конечного пользователя:

```
1. Получите ITNELEP_Tools.exe
2. Получите service_account.json
3. Положите оба файла в одну папку
4. Запустите ITNELEP_Tools.exe
5. При первом запуске нажмите Y для установки браузеров
6. Ctrl+H → заполните логин/пароль/ID таблиц
7. Готово!
```

---

## 📞 Если что-то не работает:

1. Проверьте антивирус
2. Запустите от имени администратора
3. Проверьте что service_account.json на месте
4. Проверьте подключение к интернету (для первого запуска)
5. Убедитесь что настройки заполнены (Ctrl+H)
