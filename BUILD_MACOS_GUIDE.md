# 🍎 Сборка для macOS

## 📱 Что получится:

Приложение **ITNELEP Tools.app** (~150-200 MB), которое:
- ✅ Содержит Python и все библиотеки
- ✅ Работает на любом Mac (macOS 10.13+)
- ✅ Не требует установки Python
- ✅ Можно передавать на другие Mac
- ✅ При первом запуске установит Playwright браузеры

---

## 🛠️ Как создать .app (на Mac):

### Требования для сборки:

- ✅ macOS 10.13 (High Sierra) или новее
- ✅ Python 3.8+
- ✅ Все зависимости установлены

### Шаг 1: Установите зависимости

```bash
# Установите Python пакеты
pip3 install -r requirements.txt

# Установите PyInstaller
pip3 install pyinstaller
```

### Шаг 2: Соберите .app

#### Способ А: Автоматическая сборка (рекомендуется)

```bash
./build_macos.sh
```

#### Способ Б: Ручная сборка

```bash
pyinstaller build_macos.spec --clean
mv "dist/ITNELEP Tools.app" .
```

### Шаг 3: Готово!

После сборки вы получите:
```
ITNELEP Tools.app (~150-200 MB)
```

---

## 📦 Что передать пользователям:

### Минимальный набор:
```
📁 ITNELEP_Tools/
  ├── ITNELEP Tools.app
  └── service_account.json
```

### С инструкцией:
```
📁 ITNELEP_Tools_macOS/
  ├── ITNELEP Tools.app
  ├── service_account.json
  └── README_MACOS.md
```

Упакуйте в ZIP или DMG и передавайте!

---

## 🚀 Как пользователи запускают:

### Первый запуск:

1. **Скачать и распаковать**
   - Распаковать ZIP архив
   - Перетащить "ITNELEP Tools.app" в папку "Программы" (опционально)

2. **Первый запуск - обход Gatekeeper**
   
   При первом запуске macOS может показать:
   ```
   "ITNELEP Tools.app" не может быть открыт, так как 
   разработчик не может быть проверен.
   ```
   
   **Решение:**
   - Откройте **Finder** → найдите приложение
   - Зажмите **Control** + клик на приложение
   - Выберите **"Открыть"**
   - В диалоге нажмите **"Открыть"**
   
   Или через **Системные настройки**:
   - **Системные настройки** → **Защита и безопасность**
   - Внизу нажмите **"Открыть в любом случае"**

3. **Установка браузеров**
   - При первом запуске появится диалог
   - Нажмите **"Yes"** для установки Playwright браузеров
   - Подождите 2-3 минуты

4. **Настройка**
   - Нажмите **⌘ + H** (или Ctrl+H)
   - Заполните:
     - Логин ITNELEP
     - Пароль ITNELEP
     - ID Google таблицы
   - Сохраните

5. **Готово!** Все вкладки работают

### Последующие запуски:

Просто двойной клик на **ITNELEP Tools.app** - всё уже настроено!

---

## 💻 Требования на целевом Mac:

### ✅ Нужно:
- macOS 10.13 (High Sierra) или новее
- ~500 MB свободного места
- Интернет (для первого запуска - установка браузеров)

### ❌ НЕ нужно:
- ❌ Python
- ❌ pip
- ❌ Homebrew
- ❌ Xcode
- ❌ Установка библиотек
- ❌ Терминал

---

## 🔧 Особенности macOS:

### 1. Gatekeeper (защита macOS)

macOS блокирует приложения от неизвестных разработчиков.

**Решение для пользователей:**
```
Control + клик → Открыть → Открыть
```

**Решение для разработчика (подписание):**
```bash
# Нужен Apple Developer Account ($99/год)
codesign --deep --force --sign "Developer ID Application: Your Name" "ITNELEP Tools.app"
```

### 2. Права доступа

При первом запуске macOS может запросить разрешения:
- ✅ Доступ к файлам
- ✅ Доступ к интернету
- ✅ Доступ к папкам

Это нормально - разрешите все запросы.

### 3. Размещение файла service_account.json

Файл должен быть в той же папке что и .app:
```
📁 /Applications/
  ├── ITNELEP Tools.app
  └── service_account.json  ← Здесь!
```

Или в любой другой папке, главное - рядом с .app.

---

## 📋 Сравнение с Windows:

| Параметр | Windows | macOS |
|----------|---------|-------|
| Формат | .exe | .app |
| Размер | ~200 MB | ~200 MB |
| Требования | Windows 7+ | macOS 10.13+ |
| Python нужен? | ❌ Нет | ❌ Нет |
| Первый запуск | Сразу работает | Нужно разрешить в Gatekeeper |
| Распространение | ZIP | ZIP или DMG |

---

## 🎯 Создание DMG (опционально)

Для более профессионального распространения можно создать DMG:

```bash
# Создать папку
mkdir dmg_tmp
cp -r "ITNELEP Tools.app" dmg_tmp/
cp service_account.json dmg_tmp/
cp README_MACOS.md dmg_tmp/

# Создать DMG
hdiutil create -volname "ITNELEP Tools" -srcfolder dmg_tmp -ov -format UDZO ITNELEP_Tools.dmg

# Очистить
rm -rf dmg_tmp
```

Результат: **ITNELEP_Tools.dmg** - красивый установочный образ!

---

## ⚠️ Troubleshooting:

### Ошибка: "Python not found"

```bash
# Проверьте версию Python
python3 --version

# Если нет - установите
brew install python@3.11
```

### Ошибка при сборке: "PyInstaller not found"

```bash
pip3 install pyinstaller
```

### .app не запускается

1. Проверьте права:
   ```bash
   chmod -R 755 "ITNELEP Tools.app"
   ```

2. Проверьте логи:
   ```bash
   # Запустите из терминала чтобы увидеть ошибки
   open "ITNELEP Tools.app"
   ```

### Gatekeeper блокирует

```bash
# Временно отключить (НЕ рекомендуется)
sudo spctl --master-disable

# Разрешить конкретное приложение
xattr -cr "ITNELEP Tools.app"
```

---

## 📝 Примечания:

1. **Apple Silicon (M1/M2/M3)**
   - Приложение работает на Apple Silicon через Rosetta 2
   - Для нативной поддержки нужно собирать на Mac с M-чипом

2. **Подписание кода**
   - Без подписи пользователи увидят предупреждение
   - С подписью ($99/год) всё работает сразу

3. **Нотаризация**
   - Для распространения вне App Store рекомендуется нотаризация
   - Требует Apple Developer Account

4. **Размер приложения**
   - .app содержит весь Python + все библиотеки
   - Это нормально для standalone приложений

---

## ✅ Контрольный список сборки:

- [ ] macOS 10.13+ с Python 3.8+
- [ ] Установлены зависимости: `pip3 install -r requirements.txt`
- [ ] Установлен PyInstaller: `pip3 install pyinstaller`
- [ ] Запущена сборка: `./build_macos.sh`
- [ ] Получен файл "ITNELEP Tools.app"
- [ ] Протестирован запуск
- [ ] service_account.json рядом с .app
- [ ] Готов к передаче пользователям!

---

## 🎉 Готово!

Теперь у вас есть полноценное macOS приложение, которое:
- ✅ Работает на любом Mac
- ✅ Не требует Python
- ✅ Легко распространяется
- ✅ Профессионально выглядит

**Для Windows используйте build_exe.bat**
**Для macOS используйте build_macos.sh**

Универсальное приложение для всех платформ! 🚀
