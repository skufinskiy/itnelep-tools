# 🔧 Решение проблем

## Проблема: Ошибки кодировки в Windows CMD

### Симптомы:
```
'РђРІС‚РѕСЂРёР·Р°С†РёСЏ' is not recognized as an internal or external command
'import PyQt5; print("В PyQt5 СѓСЃС‚Р°РЅРѕРІР»РµРЅ")' is not recognized...
```

### Решение 1: Используйте start.py (РЕКОМЕНДУЕТСЯ)
Вместо START.bat используйте Python-запускальщик:

```bash
python start.py
```

Этот файл:
- ✅ Проверяет все зависимости
- ✅ Показывает понятные сообщения об ошибках
- ✅ Не имеет проблем с кодировкой

### Решение 2: Прямой запуск
Просто запустите главный файл напрямую:

```bash
python unified_app.py
```

### Решение 3: Измените кодировку CMD (если нужны .bat файлы)
1. Откройте CMD от имени администратора
2. Выполните:
```bash
chcp 65001
```
3. Теперь запустите START.bat

### Решение 4: Используйте PowerShell
Вместо CMD используйте PowerShell:
```powershell
python unified_app.py
```

---

## Проблема: "Python не найден"

### Решение:
1. **Установите Python 3.8+** с официального сайта:
   https://www.python.org/downloads/

2. **При установке обязательно отметьте:**
   ☑ Add Python to PATH

3. **Проверьте установку:**
   ```bash
   python --version
   ```

---

## Проблема: "Module not found"

### Симптомы:
```
ModuleNotFoundError: No module named 'PyQt5'
ModuleNotFoundError: No module named 'selenium'
```

### Решение:
Установите зависимости:

```bash
# Установить все пакеты
pip install -r requirements.txt

# Установить Playwright браузеры
playwright install chromium

# Или установите вручную:
pip install PyQt5
pip install selenium
pip install playwright
pip install gspread
pip install google-auth
pip install oauth2client
pip install requests
pip install pandas
pip install openai
pip install pymorphy2
```

---

## Проблема: "Failed to connect to Google Sheets"

### Симптомы:
```
❌ Ошибка подключения к Google Sheets
```

### Решение:

1. **Проверьте файл service_account.json:**
   - Файл должен быть в корневой папке
   - Файл должен содержать валидные ключи

2. **Предоставьте доступ к таблицам:**
   - Откройте service_account.json
   - Найдите "client_email" (например: finance-skuf@project.iam.gserviceaccount.com)
   - Откройте вашу Google таблицу
   - Нажмите "Поделиться"
   - Добавьте этот email с правами "Редактор"

3. **Проверьте ID таблицы:**
   - Откройте настройки (Ctrl+H)
   - Проверьте "ID таблицы"
   - ID берется из URL: `https://docs.google.com/spreadsheets/d/{ID}/edit`

---

## Проблема: "Не удалось войти на сайт"

### Симптомы:
```
❌ Не удалось войти на сайт
```

### Решение:

1. **Проверьте логин и пароль:**
   - Нажмите Ctrl+H
   - Вкладка "Авторизация"
   - Убедитесь, что логин и пароль правильные
   - Сохраните настройки

2. **Проверьте доступность сайта:**
   - Откройте https://api.itnelep.com в браузере
   - Убедитесь, что сайт работает

3. **Попробуйте вручную:**
   - Попробуйте войти в браузере вручную
   - Если не получается - проблема с данными для входа

---

## Проблема: ChromeDriver / Playwright browser не найден

### Для Selenium (Parser Tab):
ChromeDriver устанавливается автоматически через webdriver-manager.
При первом запуске может занять время.

### Для Playwright (Renamer, Greeting, Obrezka):

```bash
# Установить браузер
playwright install chromium

# Если не помогает, попробуйте:
playwright install
```

---

## Проблема: "API Key invalid" (DaData или OpenAI)

### Решение:

1. **Откройте настройки (Ctrl+H)**

2. **Вкладка "API":**
   - **DaData Token** - получить на https://dadata.ru/
   - **OpenAI API Key** - получить на https://platform.openai.com/

3. **Проверьте ключи:**
   - Убедитесь, что ключи скопированы полностью
   - Проверьте, что нет лишних пробелов
   - Проверьте срок действия ключей

---

## Проблема: Приложение зависает

### Возможные причины:

1. **Headless режим:**
   - Попробуйте отключить "Headless" в настройках
   - Так вы увидите, что происходит в браузере

2. **Сайт не отвечает:**
   - Проверьте соединение с интернетом
   - Попробуйте открыть сайт в браузере

3. **Слишком много запросов:**
   - Увеличьте задержку между запросами
   - Обрабатывайте меньше строк за раз

---

## Проблема: "Permission denied" / "Access denied"

### Решение:

1. **Запустите от имени администратора:**
   - Правый клик на CMD или PowerShell
   - "Запуск от имени администратора"

2. **Проверьте антивирус:**
   - Некоторые антивирусы блокируют автоматизацию браузера
   - Добавьте папку в исключения

---

## Общие советы

### ✅ Проверка перед запуском:

1. **Python установлен?**
   ```bash
   python --version
   ```
   Должно быть 3.8 или выше

2. **Зависимости установлены?**
   ```bash
   pip list | findstr PyQt5
   pip list | findstr selenium
   pip list | findstr playwright
   ```

3. **Файлы на месте?**
   - service_account.json
   - config.json
   - google_api.py
   - scraper.py
   - unified_app.py

### 🔍 Отладка:

1. **Включите логи:**
   - Все вкладки имеют секцию "Логи"
   - Внимательно читайте сообщения об ошибках

2. **Проверьте config.json:**
   - Откройте файл в текстовом редакторе
   - Убедитесь, что все поля заполнены

3. **Тестируйте по одной вкладке:**
   - Начните с простой задачи
   - Убедитесь, что базовая функциональность работает

---

## Контрольный список запуска

- [ ] Python 3.8+ установлен
- [ ] Команда `python --version` работает
- [ ] Выполнена команда `pip install -r requirements.txt`
- [ ] Выполнена команда `playwright install chromium`
- [ ] Файл service_account.json в корневой папке
- [ ] Service account имеет доступ к Google таблицам
- [ ] Настройки заполнены (Ctrl+H)
- [ ] Логин и пароль ITNELEP указаны
- [ ] ID таблиц правильные

---

## Если ничего не помогло

1. **Удалите все и начните заново:**
   ```bash
   # Удалите папку
   # Распакуйте архив заново
   # Установите зависимости:
   pip install -r requirements.txt
   playwright install chromium
   ```

2. **Используйте start.py вместо .bat:**
   ```bash
   python start.py
   ```

3. **Проверьте логи приложения:**
   - Читайте сообщения в секции "Логи" каждой вкладки
   - Там указана конкретная причина ошибки

---

## Быстрый старт без ошибок

Самый надежный способ запуска:

```bash
# 1. Установить зависимости
pip install -r requirements.txt
playwright install chromium

# 2. Запустить через Python (НЕ через .bat!)
python start.py

# ИЛИ напрямую
python unified_app.py

# 3. Настроить (Ctrl+H)
# 4. Готово!
```
