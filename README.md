# Unified ITNELEP Tools

Объединённое приложение с 4 вкладками для работы с ITNELEP.

## 🎯 Возможности

### 1️⃣ Вкладка "Parser + Sheets"
- Парсинг данных ITNELEP
- Применение фильтров (старые, без заметок, минимальный депозит)
- Автоматическая запись в Google Sheets
- Поддержка различных листов (1кк, 500к, 0, 2кк дальняк, У чатеров)

### 2️⃣ Вкладка "Переименование ИНН"
- Автоматическое переименование ИНН на основе данных DaData
- Обновление названий в Google Sheets
- История обработки с возможностью отмены

### 3️⃣ Вкладка "Генератор приветствий"
- Генерация персонализированных приветствий
- Интеграция с OpenAI для улучшения текстов
- Автоматическая вставка в ITNELEP
- Поддержка различных вариантов обращений

### 4️⃣ Вкладка "Обрезка возраста"
- Автоматическая установка диапазона возраста для ИНН
- Обработка списков из Google Sheets
- Автоматическая авторизация на сайте
- Отслеживание обработанных записей

## 📦 Установка

### Требования
- Python 3.8 или выше
- Google Chrome (для Selenium)
- Chromium/Edge (для Playwright)

### Шаги установки

1. **Установите Python зависимости:**
```bash
pip install -r requirements.txt
```

2. **Установите Playwright браузеры:**
```bash
playwright install chromium
```

3. **Настройте Google Sheets API:**
   - Поместите файл `service_account.json` в корневую директорию
   - Предоставьте доступ service account к вашим Google Sheets

4. **Настройте конфигурацию:**
   - Откройте приложение
   - Нажмите `Ctrl+H` для открытия настроек
   - Заполните необходимые поля:
     - Логин и пароль ITNELEP
     - ID Google таблиц
     - API ключи (DaData, OpenAI)

## 🚀 Запуск

```bash
python unified_app.py
```

## ⚙️ Настройки (Ctrl+H)

### Вкладка "Авторизация"
- **Логин ITNELEP** - логин для входа на сайт
- **Пароль** - пароль для входа на сайт

### Вкладка "Google Sheets"
- **ID таблицы (Parser)** - ID основной таблицы для парсера
- **ID таблицы с ИНН** - ID таблицы для генератора приветствий
- **ID таблицы маппинга** - ID таблицы с маппингом ИНН → ID
- **Название листа маппинга** - обычно "Айди"
- **Service Account файл** - путь к файлу с ключами Google API

### Вкладка "API"
- **DaData Token** - токен для API DaData (для вкладки "Переименование")
- **OpenAI API Key** - ключ API OpenAI (для вкладки "Генератор приветствий")

## 📋 Использование

### Вкладка "Parser + Sheets"

1. Выберите нужный лист из выпадающего списка
2. Выберите строки для обработки (Ctrl+клик для множественного выбора)
3. Настройте фильтры при необходимости
4. Нажмите "▶ Обработать"
5. Следите за прогрессом в логах

### Вкладка "Переименование ИНН"

1. Приложение автоматически загрузит ИНН из таблицы
2. Выберите ИНН для обработки
3. Нажмите "Начать переименование"
4. Система автоматически получит названия из DaData и обновит таблицу

### Вкладка "Генератор приветствий"

1. Выберите лист с ИНН
2. Выберите лидера для обращения
3. Настройте параметры генерации
4. Нажмите "Сгенерировать приветствия"
5. Проверьте результаты и вставьте их на сайт

### Вкладка "Обрезка возраста"

1. Настройте диапазон годов рождения (по умолчанию 1925-1971)
2. Установите задержку между запросами
3. Выберите количество повторов при ошибке
4. Нажмите "▶ Старт"
5. Используйте "⏸ Пауза" и "▶ Продолжить" для управления процессом

## 🎨 Темы оформления

Приложение поддерживает светлую и тёмную темы:
- Переключение через меню "Вид" → "Светлая тема"

## 🔧 Горячие клавиши

- `Ctrl+H` - Открыть настройки

## 📝 Файлы и структура

```
unified_itnelep_app/
├── unified_app.py              # Главный файл приложения
├── config.json                 # Файл конфигурации
├── service_account.json        # Ключи Google API
├── credentials.json            # Копия ключей Google API
├── processed_inns.txt          # Обработанные ИНН (для обрезки возраста)
├── processed_flows.json        # История обработки (для переименования)
├── google_api.py               # Модуль для работы с Google Sheets
├── scraper.py                  # Модуль для парсинга ITNELEP
├── requirements.txt            # Python зависимости
└── tabs/
    ├── __init__.py
    ├── parser_tab.py           # Вкладка парсера
    ├── renamer_tab.py          # Вкладка переименования
    ├── greeting_tab.py         # Вкладка генератора приветствий
    ├── obrezka_tab.py          # Вкладка обрезки возраста
    └── settings_dialog.py      # Диалог настроек
```

## ⚠️ Важные примечания

1. **Логин и пароль хранятся в настройках** - не нужно вводить их каждый раз
2. **Service Account должен иметь доступ** к вашим Google таблицам
3. **Playwright профиль** сохраняет сессию для вкладок "Переименование", "Приветствия" и "Обрезка"
4. **Обработанные ИНН** сохраняются в `processed_inns.txt` для избежания дублирования

## 🐛 Решение проблем

### Ошибка "Не удалось подключиться к Google Sheets"
- Проверьте, что файл `service_account.json` находится в корневой директории
- Убедитесь, что service account имеет доступ к таблице

### Ошибка "Не удалось войти на сайт"
- Проверьте логин и пароль в настройках (Ctrl+H)
- Убедитесь, что сайт ITNELEP доступен

### Playwright не находит браузер
```bash
playwright install chromium
```

### Selenium не находит ChromeDriver
- ChromeDriver устанавливается автоматически через webdriver-manager
- При первом запуске может потребоваться время на скачивание

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в соответствующей вкладке
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки (Ctrl+H)

## 📄 Лицензия

Внутренний инструмент для работы с ITNELEP.
