# 🚀 GitHub Actions - Пошаговая настройка

## 📋 Что вы получите:

После настройки:
- ✅ Автоматическая сборка `.exe` для Windows
- ✅ Автоматическая сборка `.app` для macOS
- ✅ Создание `.dmg` установщика для Mac
- ✅ Готовые файлы доступны для скачивания
- ✅ При каждом release - автоматически собираются оба файла

**Время настройки:** 10-15 минут
**Требования:** Аккаунт на GitHub (бесплатный)

---

## 🎯 Шаг 1: Создание GitHub аккаунта

### Если у вас уже есть аккаунт - пропустите этот шаг

1. Перейдите на https://github.com
2. Нажмите **"Sign up"** (Зарегистрироваться)
3. Введите:
   - Email
   - Пароль
   - Username (имя пользователя)
4. Подтвердите email
5. Готово! ✅

---

## 📦 Шаг 2: Создание репозитория

### 2.1 Создайте новый репозиторий

1. Войдите в GitHub
2. Нажмите **"+"** в правом верхнем углу
3. Выберите **"New repository"**

### 2.2 Заполните форму:

```
Repository name: itnelep-tools
Description: ITNELEP Tools - Unified Application
Public (✓) - для бесплатного GitHub Actions
[ ] Add a README file - НЕ отмечайте (у нас уже есть)
[ ] Add .gitignore - НЕ отмечайте
[ ] Choose a license - можете выбрать MIT (опционально)
```

4. Нажмите **"Create repository"**

---

## 💻 Шаг 3: Загрузка кода в GitHub

### Вариант А: Через командную строку (рекомендуется)

#### 3.1 Установите Git (если ещё не установлен)

**Windows:**
- Скачайте: https://git-scm.com/download/win
- Установите с настройками по умолчанию

**macOS:**
- Git уже установлен
- Или через Homebrew: `brew install git`

#### 3.2 Откройте терминал/командную строку

**Windows:**
- Нажмите Win+R
- Введите `cmd` и Enter
- ИЛИ откройте PowerShell

**macOS:**
- Нажмите Cmd+Space
- Введите `Terminal` и Enter

#### 3.3 Перейдите в папку с проектом

```bash
# Windows пример
cd C:\Users\YourName\Downloads\unified_app_final

# macOS пример
cd ~/Downloads/unified_app_final
```

#### 3.4 Инициализируйте Git репозиторий

```bash
git init
```

#### 3.5 Добавьте все файлы

```bash
git add .
```

#### 3.6 Создайте первый коммит

```bash
git commit -m "Initial commit - ITNELEP Tools v2.0"
```

#### 3.7 Привяжите к GitHub репозиторию

**ВАЖНО:** Замените `YOUR_USERNAME` на ваш GitHub username!

```bash
git remote add origin https://github.com/YOUR_USERNAME/itnelep-tools.git
```

Пример:
```bash
git remote add origin https://github.com/ivanov/itnelep-tools.git
```

#### 3.8 Загрузите код на GitHub

```bash
git branch -M main
git push -u origin main
```

**При первой загрузке** GitHub попросит авторизоваться:
- Введите ваш GitHub username
- Введите пароль (или Personal Access Token - см. ниже)

---

### Вариант Б: Через GitHub Desktop (проще для новичков)

#### 3.1 Установите GitHub Desktop

- Скачайте: https://desktop.github.com
- Установите и войдите в аккаунт

#### 3.2 Добавьте проект

1. **File** → **Add local repository**
2. Выберите папку `unified_app_final`
3. GitHub Desktop скажет "This directory does not appear to be a Git repository"
4. Нажмите **"Create a repository"**

#### 3.3 Создайте коммит

1. В левой панели увидите все файлы
2. Внизу введите:
   - Summary: `Initial commit`
   - Description: `ITNELEP Tools v2.0`
3. Нажмите **"Commit to main"**

#### 3.4 Опубликуйте на GitHub

1. Нажмите **"Publish repository"**
2. Выберите:
   - Name: `itnelep-tools`
   - [ ] Keep this code private - НЕ отмечайте
3. Нажмите **"Publish repository"**

---

### Вариант В: Через веб-интерфейс GitHub (самый простой, но медленный)

1. Откройте ваш новый репозиторий на GitHub
2. Нажмите **"uploading an existing file"**
3. Перетащите ВСЕ файлы из папки `unified_app_final`
4. Подождите загрузки
5. Нажмите **"Commit changes"**

---

## 🔑 Шаг 4: Personal Access Token (если нужен)

### Если Git просит пароль при `git push`:

С 2021 года GitHub требует Personal Access Token вместо пароля.

#### 4.1 Создайте токен

1. GitHub → **Settings** (ваш профиль)
2. В меню слева → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. Нажмите **"Generate new token"** → **"Generate new token (classic)"**

#### 4.2 Настройте токен

```
Note: Git access for ITNELEP Tools
Expiration: No expiration (или 90 days)

Scopes:
[✓] repo (полный доступ к репозиториям)
```

5. Нажмите **"Generate token"**
6. **СКОПИРУЙТЕ ТОКЕН!** (он показывается только один раз)

#### 4.3 Используйте токен

При запросе пароля:
- Username: ваш GitHub username
- Password: **ВСТАВЬТЕ ТОКЕН** (не обычный пароль!)

---

## ✅ Шаг 5: Проверка настройки

### 5.1 Откройте репозиторий на GitHub

```
https://github.com/YOUR_USERNAME/itnelep-tools
```

### 5.2 Проверьте что всё загружено

Вы должны увидеть:
- ✅ Все файлы проекта
- ✅ Папку `.github/workflows/`
- ✅ Файл `build.yml` внутри

### 5.3 Перейдите во вкладку Actions

1. В репозитории нажмите **"Actions"**
2. Вы увидите сообщение: "Get started with GitHub Actions"
3. Это нормально - workflow запустится при создании tag

---

## 🏷️ Шаг 6: Создание первого release

### Теперь создадим release - это запустит автоматическую сборку!

#### 6.1 Через командную строку (рекомендуется)

```bash
# Создайте tag
git tag v1.0.0

# Загрузите tag на GitHub
git push origin v1.0.0
```

#### 6.2 Через GitHub Desktop

1. **Repository** → **Create Tag**
2. Tag name: `v1.0.0`
3. Target: `main`
4. Нажмите **"Create Tag"**
5. Нажмите **"Push origin"**

#### 6.3 Через веб-интерфейс GitHub

1. В репозитории → **Releases** (справа)
2. Нажмите **"Create a new release"**
3. **"Choose a tag"** → введите `v1.0.0` → **"+ Create new tag: v1.0.0 on publish"**
4. Release title: `v1.0.0 - First Release`
5. Description: Можете оставить пустым или написать что-то
6. Нажмите **"Publish release"**

---

## 🎬 Шаг 7: Отслеживание сборки

### 7.1 Перейдите во вкладку Actions

После создания tag автоматически запустится сборка!

```
GitHub → Ваш репозиторий → Actions
```

### 7.2 Вы увидите workflow "Build ITNELEP Tools"

Статусы:
- 🟡 **Жёлтый кружок** - сборка идёт
- ✅ **Зелёная галочка** - успех
- ❌ **Красный крестик** - ошибка

### 7.3 Нажмите на workflow чтобы увидеть детали

Вы увидите три задачи (jobs):
1. **build-windows** - сборка .exe
2. **build-macos** - сборка .app
3. **create-release** - создание release

### 7.4 Подождите ~10-15 минут

Каждая сборка займёт 5-7 минут.

---

## 📥 Шаг 8: Скачивание готовых файлов

### После успешной сборки:

#### 8.1 Перейдите в Releases

```
GitHub → Ваш репозиторий → Releases
```

#### 8.2 Вы увидите release "v1.0.0"

Под ним будут файлы:

**Assets:**
- 📦 `ITNELEP_Tools_Windows.zip` (~200 MB)
  - Содержит: ITNELEP_Tools.exe + service_account.json
  
- 💿 `ITNELEP_Tools.dmg` (~200 MB)
  - Установщик для macOS

#### 8.3 Скачайте нужные файлы

- Для Windows пользователей → `ITNELEP_Tools_Windows.zip`
- Для Mac пользователей → `ITNELEP_Tools.dmg`

#### 8.4 Готово! 🎉

Теперь у вас есть оба файла без необходимости второго компьютера!

---

## 🔄 Шаг 9: Последующие releases

### При каждом обновлении:

#### 9.1 Внесите изменения в код

```bash
# Отредактируйте файлы
# ...

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Update: добавлена новая функция"

# Загрузите на GitHub
git push
```

#### 9.2 Создайте новый tag для release

```bash
# Создайте новую версию
git tag v1.1.0

# Загрузите tag
git push origin v1.1.0
```

#### 9.3 Автоматически соберутся новые версии!

GitHub Actions автоматически:
- ✅ Соберёт новый .exe
- ✅ Соберёт новый .app
- ✅ Создаст новый release "v1.1.0"

---

## 🔧 Troubleshooting

### Проблема 1: Сборка упала с ошибкой (красный крестик)

**Решение:**
1. Перейдите в Actions → кликните на упавший workflow
2. Откройте упавшую задачу (build-windows или build-macos)
3. Посмотрите логи - там будет ошибка
4. Часто проблема:
   - Отсутствует файл в репозитории
   - Неправильный путь в .spec файле
   - Ошибка в зависимостях

### Проблема 2: Git просит пароль постоянно

**Решение:**
```bash
# Сохраните credentials
git config --global credential.helper store

# При следующем push введите токен - он сохранится
```

### Проблема 3: "Permission denied" при push

**Решение:**
- Проверьте что вы владелец репозитория
- Проверьте что Personal Access Token имеет права "repo"
- Попробуйте создать новый токен

### Проблема 4: Actions не запускается

**Проверьте:**
1. Файл `.github/workflows/build.yml` загружен на GitHub
2. Репозиторий публичный (для бесплатного Actions)
3. Tag создан правильно: `v1.0.0` (с буквой v)

### Проблема 5: Файлы не появляются в Release

**Решение:**
1. Убедитесь что все три задачи (jobs) завершились успешно
2. Проверьте что tag начинается с "v" (например, v1.0.0)
3. Посмотрите логи задачи "create-release"

---

## 💡 Полезные советы

### Версионирование

Используйте семантическое версионирование:
- `v1.0.0` - первый релиз
- `v1.0.1` - маленький баг-фикс
- `v1.1.0` - новая функция
- `v2.0.0` - большие изменения

### Приватный репозиторий

Если репозиторий приватный:
- Бесплатно: 2000 минут GitHub Actions в месяц
- Сборка занимает ~20 минут (оба файла)
- Хватит на ~100 сборок в месяц

### Отключение Actions для экономии минут

Если не нужна автоматическая сборка:
1. **Settings** → **Actions** → **General**
2. **Actions permissions** → **Disable Actions**

### Ручная сборка через Actions

Можно запустить сборку вручную:
1. **Actions** → **Build ITNELEP Tools**
2. **Run workflow** → **Run workflow**

---

## 📊 Примерное время

| Этап | Время |
|------|-------|
| Создание GitHub аккаунта | 5 минут |
| Создание репозитория | 2 минуты |
| Загрузка кода | 5 минут |
| Создание release | 2 минуты |
| **Ожидание сборки** | **10-15 минут** |
| Скачивание файлов | 2 минуты |
| **Итого** | **~30 минут** |

**Последующие releases:** 2 минуты + 10-15 минут ожидания

---

## ✅ Контрольный список

Проверьте что выполнили все шаги:

- [ ] Создан GitHub аккаунт
- [ ] Создан репозиторий `itnelep-tools`
- [ ] Весь код загружен на GitHub
- [ ] Файл `.github/workflows/build.yml` присутствует
- [ ] Создан tag `v1.0.0`
- [ ] Workflow запустился (вкладка Actions)
- [ ] Все три задачи завершились успешно ✅
- [ ] В Releases появились файлы:
  - [ ] `ITNELEP_Tools_Windows.zip`
  - [ ] `ITNELEP_Tools.dmg`
- [ ] Скачаны и протестированы оба файла

---

## 🎉 Готово!

**Теперь у вас:**
- ✅ Автоматическая сборка .exe и .app
- ✅ При каждом release оба файла создаются автоматически
- ✅ Не нужен второй компьютер!
- ✅ Профессиональный workflow разработки

**При каждом обновлении:**
```bash
git add .
git commit -m "Update: описание изменений"
git push
git tag v1.x.x
git push origin v1.x.x
```

И через 15 минут у вас готовые .exe и .app! 🚀
